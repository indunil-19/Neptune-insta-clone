const config = {
  choreoApiUrl: window?.configs?.serviceURL ? window.configs.serviceURL : "/",
  cloudinaryApiUrl: "https://api.cloudinary.com/v1_1/dm36weewi/image/upload",
  upload_preset: "insta_clone",
  cloud_name: "dm36weewi",
};

export default config;
